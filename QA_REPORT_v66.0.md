# 흥양기 v66.0 여자만 탐험 검수

## 기획 정합성

- 기획 정본의 `현실 이동 → 단서 관찰 → 방법 검증 → 기록 → 이면 진입 → 3구역 조사 → 네임드 등장 기록` 순서를 구현했다.
- 여자만은 실제 고흥 해안과 갯벌을 현실층으로 두고, 이면층은 수로와 물때의 거리·반복만 어긋나도록 구성했다.
- 중국풍 건축·복식·장식 없이 한국 남해안 어촌과 갯벌의 생활 풍경을 기준으로 원화를 교체했다.

## 자동 완주 검수

- Android 가로 화면과 같은 1279×599 뷰포트에서 여자만 전 과정을 자동 플레이했다.
- 이동 4구간, 현실 단서 3개, 이면 구역 3개, 갯마리 등장 기록을 모두 통과했다.
- 단서 버튼 최소 높이 48px, 번호와 제목 표시, 조사 패널·상단 정보·보스 패널 화면 내 배치를 확인했다.
- 이동·탐색·이면·보스 원화 4장 모두 1672×941 로드와 표시를 확인했다.
- 페이지 오류, 404 응답, 콘솔 오류는 0건이었다.

검수 명령:

```sh
npm run qa:mobile
npm run qa:yeojaman
```
