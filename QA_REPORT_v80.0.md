# 흥양기 v80.0 검수 보고서

## 수정 내용

- 이동, 현실 탐사, 지역 던전, 보스 등장 화면에 `메인으로` 직접 복귀 버튼을 추가했다.
- 지역 던전에서는 `메인으로 나가기`를 별도 한 줄 버튼으로 배치해 `현실 탐색으로`, `지도`와 겹치지 않게 했다.
- 보스전 승리·패배 결과창에서도 메인 화면으로 바로 복귀할 수 있다.
- 탐험 화면 종료 시 iframe을 `about:blank`로 비우고 로딩 타이머와 활성 지역 상태를 정리한다.
- 기존 `지도` 버튼은 유지하며 탐험 진행 기록은 복귀 방식과 관계없이 보존한다.

## 자동 검수 항목

- Android 가로 화면 1279×599에서 이동 단계 `메인으로` 버튼의 화면 내부 배치와 터치 크기 확인
- 같은 해상도의 나로도 지역 던전에서 `메인으로 나가기` 버튼의 화면 내부 배치, 터치 크기, 인접 버튼 비중첩 확인
- 두 버튼 클릭 시 활성 화면이 `home`으로 바뀌고 탐험 iframe이 `about:blank`로 정리되는지 확인
- 50회 화면 라우팅, 125장 카드·6단계 스킬, 상점·인벤토리·파티·도감 회귀 검수
- 5개 지역 여행·15개 단서 장면·7개 던전 구간·보스전·7성 현현 회귀 검수

## 배포 판정

- 모바일 UI·직접 복귀: 통과
- 5개 지역 이동·탐사·던전: 통과
- 5개 지역 보스전·7성 현현: 통과
- 카드 성장·보상·제작: 통과
- 카드 125종 현현 프로필: 통과
- 콘솔·HTTP 오류: 0건
- 위 검수를 통과한 커밋을 `main`에 반영하고 GitHub Pages 배포 완료 후 공개 버전 `80.0.0`을 재확인한다.
